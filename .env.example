# Gig workspace env — copy to .env (frontend reads VITE_* vars).
# Mock mode is the default: the full UI runs with no backend at all.

# Point at the local backend and every section goes live:
VITE_API_BASE_URL=http://localhost:4000/api
VITE_USE_MOCKS=false
